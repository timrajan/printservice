import styled from 'styled-components'

export const ContactUsWrapper = styled.div`
  margin: 1rem 0;
  margin-bottom: 2rem;
`

export const ContactUsTitle = styled.p`
  color: #1a365d;
  font-weight: 900;
  font-size: 2.15rem;
  padding-top: 1rem;
  padding-bottom: 0.2rem;
`

export const ContactUsSubtitle = styled.p`
  font-weight: 700;
  color: #2d7eff;
  font-size: 1.8rem;
  padding-top: 1rem;
  padding-bottom: 1rem;
`

export const ContactUsContentWrapper = styled.div`
  font-size: 1.2rem;
`

export const ContactUsContentTitle = styled.div`
  font-weight: 700;
  color: #1a365d;
`

export const ContactUsContent = styled.p`
  display: block;
  line-height: 1.7;
`

export const ContactUsContentLink = styled.a`
  display: inline-block;
  text-decoration: underline;
  text-underline-offset: 5px;
  color: #2d7eff;
`
